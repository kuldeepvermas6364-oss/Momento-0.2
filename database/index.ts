/**
 * /database/index.ts
 * Central export point for database-related code.
 *
 * Sub-folders:
 *   /schema     → table definitions
 *   /migrations → versioned migration files
 *   /seed       → seed data for development
 *   /queries    → reusable query functions
 *   /indexes    → performance indexes
 *   /policies   → RLS / access policies
 *
 * NOTE: Currently using Firebase RTDB. If migrating to PostgreSQL
 * (Supabase/Neon), schema and migration files will be added here.
 */

export {};
