/**
 * /features/advertisements/index.ts
 * Advertisements feature module — ad serving, ad management, analytics.
 *
 * Sub-folders:
 *   components/  → AdBanner, AdCard, AdManager
 *   hooks/       → useAds, useAdImpressions
 *   types/       → Advertisement, AdTarget, AdStats
 *   services/    → adService (fetch, track, create)
 *   utils/       → ad targeting, impression helpers
 */

export {};
