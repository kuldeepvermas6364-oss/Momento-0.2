/**
 * /features/ai/index.ts
 * AI feature module — AI-powered content, captions, recommendations, moderation.
 *
 * Sub-folders:
 *   components/  → AIAssistant, AICaptionGenerator, AIMods
 *   hooks/       → useAI, useAICaption
 *   types/       → AIResponse, AIConfig
 *   services/    → aiService (generateCaption, moderateContent, recommend)
 *   utils/       → prompt builders, AI response parsers
 */

export {};
