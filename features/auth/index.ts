/**
 * /features/auth/index.ts
 * Auth feature module — authentication, signup, login, password reset.
 *
 * Sub-folders:
 *   components/  → AuthProvider, LoginForm, SignupForm, etc.
 *   hooks/       → useAuth, useSession
 *   types/       → AuthUser, AuthSession
 *   services/    → authService (signUp, signIn, signOut, resetPassword)
 *   utils/       → token helpers, auth validators
 */

export {};
