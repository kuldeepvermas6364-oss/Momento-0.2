/**
 * /features/chat/index.ts
 * Chat feature module — real-time messaging, conversations, media messages.
 *
 * Sub-folders:
 *   components/  → ChatWindow, MessageBubble, ConversationList
 *   hooks/       → useChat, useConversations
 *   types/       → Chat, Message, Conversation
 *   services/    → chatService (sendMessage, fetchConversations)
 *   utils/       → message helpers, chat formatting
 */

export {};
