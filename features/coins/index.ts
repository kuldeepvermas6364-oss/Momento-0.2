/**
 * /features/coins/index.ts
 * Coins feature module — virtual currency, rewards, wallet, transactions.
 *
 * Sub-folders:
 *   components/  → CoinWallet, CoinTransaction, CoinReward
 *   hooks/       → useCoins, useCoinBalance
 *   types/       → Coin, CoinTransaction, CoinWallet
 *   services/    → coinService (earn, spend, transfer, fetchBalance)
 *   utils/       → coin calculations, reward helpers
 */

export {};
