/**
 * /features/feed/index.ts
 * Feed feature module — main social feed, infinite scroll, feed algorithms.
 *
 * Sub-folders:
 *   components/  → FeedCard, FeedList, FeedSkeleton
 *   hooks/       → useFeed, useInfiniteFeed
 *   types/       → FeedItem, FeedResponse
 *   services/    → feedService (fetchFeed, likePost, savePost)
 *   utils/       → feed排序, feed filters
 */

export {};
