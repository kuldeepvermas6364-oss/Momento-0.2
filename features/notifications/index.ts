/**
 * /features/notifications/index.ts
 * Notifications feature module — push notifications, activity feed, badges.
 *
 * Sub-folders:
 *   components/  → NotificationItem, NotificationList, NotificationBadge
 *   hooks/       → useNotifications, useUnreadCount
 *   types/       → Notification, NotificationSettings
 *   services/    → notificationService (fetch, markRead, registerToken)
 *   utils/       → notification formatting, priority helpers
 */

export {};
