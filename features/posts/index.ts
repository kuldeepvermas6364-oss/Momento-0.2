/**
 * /features/posts/index.ts
 * Posts feature module — create, edit, delete, like, comment, share.
 *
 * Sub-folders:
 *   components/  → PostCard, PostComposer, PostActions
 *   hooks/       → usePosts, usePostActions
 *   types/       → Post, PostMedia
 *   services/    → postService (create, update, delete, fetch)
 *   utils/       → post media processing, caption helpers
 */

export {};
