/**
 * /features/premium/index.ts
 * Premium feature module — subscriptions, premium features, billing.
 *
 * Sub-folders:
 *   components/  → PremiumBadge, SubscriptionPlans, PremiumCard
 *   hooks/       → usePremium, useSubscription
 *   types/       → Subscription, Plan, PremiumStatus
 *   services/    → premiumService (subscribe, cancel, verify)
 *   utils/       → plan helpers, billing calculations
 */

export {};
