/**
 * /features/profile/index.ts
 * Profile feature module — user profile, edit profile, followers/following.
 *
 * Sub-folders:
 *   components/  → ProfileHeader, ProfileGrid, EditProfileForm
 *   hooks/       → useProfile, useFollow
 *   types/       → Profile, FollowRelation
 *   services/    → profileService (fetch, update, follow, unfollow)
 *   utils/       → profile helpers, avatar processing
 */

export {};
