/**
 * /features/reels/index.ts
 * Reels feature module — short video creation, viewing, discovery.
 *
 * Sub-folders:
 *   components/  → ReelPlayer, ReelFeed, ReelRecorder
 *   hooks/       → useReels, useReelFeed
 *   types/       → Reel, ReelStats
 *   services/    → reelService (create, fetch, like, comment)
 *   utils/       → video processing, reel formatting
 */

export {};
