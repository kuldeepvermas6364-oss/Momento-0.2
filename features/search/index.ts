/**
 * /features/search/index.ts
 * Search feature module — user search, hashtag search, content discovery.
 *
 * Sub-folders:
 *   components/  → SearchBar, SearchResults, TrendingTags
 *   hooks/       → useSearch, useDebouncedSearch
 *   types/       → SearchResult, SearchFilters
 *   services/    → searchService (searchUsers, searchTags, searchPosts)
 *   utils/       → search helpers, query normalization
 */

export {};
