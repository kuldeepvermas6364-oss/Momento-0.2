/**
 * /features/settings/index.ts
 * Settings feature module — account settings, privacy, preferences.
 *
 * Sub-folders:
 *   components/  → SettingsList, PrivacySettings, AccountSettings
 *   hooks/       → useSettings
 *   types/       → UserSettings, PrivacyConfig
 *   services/    → settingsService (update, fetch, deleteAccount)
 *   utils/       → settings helpers
 */

export {};
