/**
 * /features/stories/index.ts
 * Stories feature module — story creation, viewing, expiration.
 *
 * Sub-folders:
 *   components/  → StoryViewer, StoryBar, StoryCreator
 *   hooks/       → useStories, useStoryViewer
 *   types/       → Story, StoryView
 *   services/    → storyService (create, fetch, markViewed)
 *   utils/       → story expiry, story media helpers
 */

export {};
