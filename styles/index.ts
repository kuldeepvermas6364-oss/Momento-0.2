/**
 * /styles/index.ts
 * Central export for global style utilities and Tailwind config
 * extensions.
 *
 * Tailwind config lives at /tailwind.config.ts (project root).
 * Global CSS lives at /app/globals.css.
 * This file re-exports any shared style tokens or CSS-in-JS
 * utilities when needed.
 */

export {};
