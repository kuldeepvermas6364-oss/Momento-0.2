/**
 * /tests/index.ts
 * Integration and end-to-end test entry point.
 *
 * Unit tests live next to source files (e.g. PostCard.test.tsx).
 * This folder holds cross-feature integration tests and shared
 * test utilities.
 *
 * Frameworks: Vitest + React Testing Library
 */

export {};
